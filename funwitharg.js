// we can pass argument (paramenters) dynamically 
// always give proper name to runction
//function sum (arg1, arg2)
 function sum (para1, para2) {
    var number1 =para1;
    var number2 =para2;
    var sumOfNumber= number1+number2;
    console.log(sumOfNumber);

 }
// calling method 
sum(1,2); // 3
sum(100,200); //300
sum (1000, 2000); //3000 


